# media-outreach 백엔드(https://github.com/VAN-AI-Innovation/media-outreach) API 주소.
# 로컬 실행 시에는 .env.local로 복사해서 실제 배포 주소를 채워 넣는다(.env.local은 *.local 패턴으로 git-ignore됨).
# 값이 없으면 http://localhost:8080으로 접속을 시도한다.
VITE_MEDIA_OUTREACH_API_URL=
