{
  "name": "newsroom-stage-1",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/generate-static-pages.js",
    "lint": "oxlint",
    "test": "node --test src/articles.test.js src/share.test.js src/mediaOutreachApi.test.js",
    "test:static": "node --test scripts/static-pages.test.js",
    "test:e2e": "playwright test --grep-invert @live",
    "check": "npm run lint && npm run test && npm run build && npm run test:static",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-datepicker": "^9.1.0",
    "react-dom": "^19.2.8",
    "react-router": "^8.3.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@vitejs/plugin-react": "^6.0.4",
    "oxlint": "^1.75.0",
    "vite": "^8.2.0"
  }
}
